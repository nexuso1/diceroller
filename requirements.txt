# Runtime dependencies for the Flask app (flask_app.py) and the calculator core.
# The legacy Streamlit UI in app.py additionally needs pandas, plotly and streamlit.
flask==3.1.3
markdown==3.10.2
numpy==2.5.1
gunicorn
